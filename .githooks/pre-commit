#!/usr/bin/env bash
set -euo pipefail

if node -e "const p = require('./package.json'); if (p.resolutions && Object.keys(p.resolutions).length > 0) { console.error('ERROR: package.json contains resolutions. Remove them before committing.'); process.exit(1); }"; then
  exit 0
else
  exit 1
fi
