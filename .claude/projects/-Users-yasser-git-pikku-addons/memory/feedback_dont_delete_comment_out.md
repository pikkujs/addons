---
name: Comment out instead of deleting
description: When disabling code temporarily, comment it out rather than deleting it
type: feedback
---

When disabling code temporarily, comment it out instead of deleting it.

**Why:** User wants to preserve the code for easy re-enabling later.

**How to apply:** When asked to "disable" or "turn off" something, default to commenting out rather than removing.
