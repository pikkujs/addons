---
'@pikku/addon-mistral': patch
---

Add @pikku/addon-mistral: document OCR (ocrProcess), OCR + structured extraction (ocrExtract), text embeddings (textEmbedding), and speech-to-text (audioTranscribe).
