# @pikku/addon-bitbucket

Bitbucket repositories and pull requests.

## Functions

- `repositoriesList` — list repositories
- `repositoriesGet` — get repository details
- `pullRequestsList` — list pull requests

## Secrets

`BITBUCKET_CREDENTIALS` — fields: username, appPassword

## Dependencies

No additional runtime dependencies.
