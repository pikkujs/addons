# @pikku/addon-jenkins

Jenkins job and build management.

## Functions

- `jobsList` — list Jenkins jobs
- `jobsTrigger` — trigger a job build
- `buildsGet` — get build details

## Secrets

`JENKINS_CREDENTIALS` — fields: username, apiKey, baseUrl

## Dependencies

No additional runtime dependencies.
