# @pikku/addon-github

GitHub repositories and issues.

## Functions

- `reposList` — list repositories
- `reposGet` — get repository details
- `issuesList` — list issues
- `issuesCreate` — create an issue

## Secrets

`GITHUB_CREDENTIALS` — fields: accessToken, server

## Dependencies

No additional runtime dependencies.
