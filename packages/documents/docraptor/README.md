# @pikku/addon-docraptor

PDF and Excel document generation via DocRaptor.

## Functions

- `documentCreate` — create a document synchronously
- `documentCreateAsync` — create a document asynchronously
- `documentGetStatus` — get async document job status

## Secrets

`DOCRAPTOR_CREDENTIALS` — DocRaptor API key (string)

## Dependencies

No additional runtime dependencies.
