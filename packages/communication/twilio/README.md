# @pikku/addon-twilio

Twilio SMS and voice calls.

## Functions

- `smsSend` — send an SMS message
- `callMake` — make a voice call

## Secrets

`TWILIO_CREDENTIALS` — fields: accountSid, authToken

## Dependencies

No additional runtime dependencies.
