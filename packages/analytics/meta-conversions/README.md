# @pikku/addon-meta-conversions

Meta (Facebook) Conversions API for server-side event tracking.

## Functions

- `eventSend` — send conversion events to Meta

## Secrets

`META_CONVERSIONS_CREDENTIALS` — fields: pixelId, accessToken

## Dependencies

No additional runtime dependencies.
