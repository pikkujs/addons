# @pikku/addon-posthog

PostHog product analytics.

## Functions

- `eventsCapture` — capture analytics events
- `personsList` — list tracked persons

## Secrets

`POSTHOG_CREDENTIALS` — fields: apiKey, host

## Dependencies

No additional runtime dependencies.
