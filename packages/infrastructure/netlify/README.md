# @pikku/addon-netlify

Netlify sites and deploy management.

## Functions

- `sitesList` — list sites
- `sitesGet` — get site details
- `deploysList` — list deploys for a site
- `deploysGet` — get deploy details

## Secrets

`NETLIFY_CREDENTIALS` — fields: apiKey

## Dependencies

No additional runtime dependencies.
