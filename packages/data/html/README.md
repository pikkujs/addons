# @pikku/addon-html

Convert HTML to text, markdown, or structured tables.

## Functions

- `htmlToText` — convert HTML to plain text
- `htmlToMarkdown` — convert HTML to markdown
- `htmlToTable` — extract tables from HTML

## Secrets

No secrets required.

## Dependencies

- html-to-text
- node-html-markdown
