# @pikku/addon-paddle

Paddle billing: products, subscriptions, and transactions.

## Functions

- `productsList` — list products
- `subscriptionsList` — list subscriptions
- `transactionsList` — list transactions

## Secrets

`PADDLE_CREDENTIALS` — fields: apiKey, sandbox

## Dependencies

No additional runtime dependencies.
