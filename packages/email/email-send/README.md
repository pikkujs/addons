# @pikku/addon-email-send

Send emails via SMTP.

## Functions

- `emailSend` — send a plain text email
- `emailSendHtml` — send an HTML email

## Secrets

`EMAIL_SEND_CREDENTIALS` — fields: host, port, user, password, secure

## Dependencies

- nodemailer
