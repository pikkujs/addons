# @pikku/addon-mailgun

Send emails via Mailgun.

## Functions

- `messageSend` — send an email message

## Secrets

`MAILGUN_CREDENTIALS` — fields: apiKey, apiDomain, emailDomain

## Dependencies

No additional runtime dependencies.
