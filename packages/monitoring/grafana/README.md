# @pikku/addon-grafana

Grafana dashboards and alert management.

## Functions

- `dashboardsList` — list dashboards
- `dashboardsGet` — get dashboard details
- `alertsList` — list alerts

## Secrets

`GRAFANA_CREDENTIALS` — fields: apiKey, baseUrl

## Dependencies

No additional runtime dependencies.
