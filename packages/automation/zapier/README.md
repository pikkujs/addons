# @pikku/addon-zapier

Trigger Zapier webhooks for workflow automation.

## Functions

- `webhookTrigger` — trigger a Zapier webhook by URL
- `webhookTriggerNamed` — trigger a named webhook from configured URLs

## Secrets

`ZAPIER_CREDENTIALS` — fields: webhookUrls (map of name to URL)

## Dependencies

No additional runtime dependencies.
