import { z } from 'zod'
import { wireSecret } from '@pikku/core/secret'

export const {{nodeName}}SecretsSchema = z.object({
  apiKey: z.string().describe('{{DISPLAY_NAME}} API key'),
  // Add other secret fields as needed
})

export type {{NodeName}}Secrets = z.infer<typeof {{nodeName}}SecretsSchema>

wireSecret({
  name: '{{nodeName}}',
  displayName: '{{DISPLAY_NAME}} API',
  description: '{{DESCRIPTION}}',
  secretId: '{{NODE_NAME_UPPER}}_CREDENTIALS',
  schema: {{nodeName}}SecretsSchema,
})
