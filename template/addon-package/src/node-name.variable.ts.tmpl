// DELETE THIS FILE if the package has no configurable variables.
// Variables are for non-secret runtime configuration (base URLs, feature flags, etc.)

import { z } from 'zod'
import { wireVariable } from '@pikku/core/variable'

export const {{nodeName}}VariableSchema = z.string().optional().describe('TODO: describe this variable')

wireVariable({
  name: '{{nodeName}}_variable',
  displayName: '{{DISPLAY_NAME}} Variable',
  description: 'TODO: describe this variable',
  variableId: '{{NODE_NAME_UPPER}}_VARIABLE',
  schema: {{nodeName}}VariableSchema,
})
